import T from 'immutable';
import invariant from 'invariant';

class ApiData {
  /**
   * @constructor
   * @param {Array<Object>} dataContracts
   */
  constructor(dataContracts) {

  }
}

export default ApiData;