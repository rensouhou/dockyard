/**
 * @overview
 *
 * @since 0.1.0
 * @author Stefan Rimaila <stefan@rimaila.fi>
 */

import BaseModel from './BaseModel';

export default class CraftItem extends BaseModel {
  constructor(data, model) {
    super(data, model);
  }
}
