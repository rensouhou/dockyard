/**
 * @overview
 *
 * @since 0.1.0
 * @author Stefan Rimaila <stefan@rimaila.fi>
 * @module
 */

import { MapStore } from 'flux/utils';
import T from 'immutable';

export default class PlayerDockStore extends MapStore {
  reduce(state, action) {

  }
}
